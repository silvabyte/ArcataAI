# envs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build envs` to build the library.

## Running unit tests

Run `nx test envs` to execute the unit tests via [Vitest](https://vitest.dev/).
