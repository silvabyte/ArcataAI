# Server
API_HOST=0.0.0.0
API_PORT=4203
CORS_ORIGINS=http://localhost:4201,http://localhost:4200

# Cron/Workflow Authentication
# Optional secret for triggering cron workflows via X-Cron-Secret header
# Leave empty to allow unauthenticated cron triggers (not recommended in production)
CRON_SECRET=

# Supabase (uses new API key format)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sb_secret_your-secret-key
# Note: JWT validation uses JWKS from SUPABASE_URL/auth/v1/.well-known/jwks.json
# Note: Use the new sb_secret_... format for SUPABASE_SERVICE_ROLE_KEY

# Authentication
# API_KEYS: Comma-separated list of valid API keys for service-to-service auth
# Leave empty or unset to disable API key authentication
API_KEYS=

# ObjectStorage (s3.audetic.link)
OBJECT_STORAGE_URL=https://s3.audetic.link/api/v1
OBJECT_STORAGE_TENANT_ID=arcata
OBJECT_STORAGE_API_KEY=your-api-key

# AI Provider (Vercel AI Gateway)
VERCEL_AI_GATEWAY_URL=https://ai-gateway.vercel.sh/v1
VERCEL_AI_GATEWAY_API_KEY=your-vercel-ai-key
AI_MODEL=anthropic/claude-haiku-4.5

# Logging
LOG_PRETTY=true
